---
- name: Playbook for tasks
  hosts: tag: env=webserver
  become: yes
  become_method: sudo
  tasks:
    - name: install multiple packages
      package:
        name: "{{item}}"
        state: present
      with_items:
        - bind-utils
        - elinks
        - curl
        - gzip

    
          
          

